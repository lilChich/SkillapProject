@model Skillap.MVC.ViewModels.MastersViewModel
@using Skillap.DAL.Interfaces

@{
    ViewData["Title"] = "Masters";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<h1>Masters</h1>

<h4>MastersViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Masters">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="Status" /> @Html.DisplayNameFor(model => model.Status)
                </label>
            </div>
            <div class="form-group">
                <label class="control-label">
                    <input asp-for="SkillLevel" class="form-control level" />
                </label>
                
                <span asp-validation-for="SkillLevel" class="text-danger"></span>
            </div>
            @*<div class="form-group">
            <label asp-for="MasterClassId" class="control-label"></label>
            <input asp-for="MasterClassId" class="form-control" />
            <span asp-validation-for="MasterClassId" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                @*<select asp-for="SelectedMasterClass" asp-items="Model.MasterClasses">
                    <option id="masterClass" value="value">Select</option>
                </select>*@
                @*@Html.DropDownListFor(m => m.MasterClassId, new SelectList(Model.MasterClasses, "Name", "Name"))*@
                <div class="form-group">
                    
                    @*@Html.DropDownListFor(m => m.MasterClasses, ViewBag.MasterClasses as SelectList, htmlAttributes: new { id = "masterClass" })*@
                </div>
                <select asp-for="SelectedMasterClass" asp-items="Model.MasterClasses">
                    <option value="value">Select</option>
                </select>
                @*@Html.DropDownList("MasterClasses", ViewBag.MasterClasses as SelectList, new { id = "masterClass" })*@
            </div>
            @*<div class="form-group">
            <label asp-for="UserId" class="control-label"></label>
            <input asp-for="UserId" class="form-control" />
            <span asp-validation-for="UserId" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <select asp-for="SelectedUser" asp-items="Model.Users">
                    <option value="value">Select</option>
                </select>
               @* @Html.DropDownList("Users", ViewBag.Users as SelectList, new { id = "user" })*@
               @* @Html.DropDownListFor(m => m.UserId, (SelectList)ViewBag.UsersSelect)*@
            </div>

            

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

@*<script type="text/javascript">
        $(function () {
            $('.level').change(function()
            {
                // получаем выбранный level
                var level = $('.level').val();
                    $.ajax({
                    type: 'GET',
                    url: '@Url.Action("GetMasterClasses")/' + level,
                    success: function (data) {

                        // заменяем содержимое присланным частичным представлением
                        $('#masterClass').replaceWith(data);
                    }
                });
            });
        })
</script>*@
}
