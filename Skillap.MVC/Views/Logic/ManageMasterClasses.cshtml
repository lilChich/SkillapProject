@using Skillap.DAL.Entities;
@using Skillap.BLL.DTO;
@model IEnumerable<MasterClassesDTO>

@{
    ViewData["Title"] = "View";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@*@if (Model.Any())
{
    <div class="container mt-lg-5">
        <div class="row">


            @foreach (var masterClass in Model)
            {
                <div class="card mb-3 mr-3 ml-3 d-flex justify-content-between">
                    <div class="card-header">
                        MasterClass Id : @masterClass.Id
                    </div>
                    <div class="card-body">
                        <h5 class="card-title">@masterClass.Name</h5>

                    </div>
                    <div class="card-body">
                        <p class="card-title">@masterClass.Description</p>

                    </div>
                    <div class="card-body">
                        <p class="card-title">@masterClass.Category</p>

                    </div>
                    <div class="card-body">
                        <p class="card-title">@masterClass.Relevance</p>

                    </div>
                    <div class="card-body">
                        <p class="card-title">@masterClass.Level</p>

                    </div>
                    <div class="card-footer">
                        <form asp-action="DeleteMasterClass" asp-route-id="@masterClass.Id" method="post">
                            <a asp-action="EditMasterClasses" asp-controller="Logic" asp-route-id="@masterClass.Id" class="btn btn-primary">Edit</a>
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>

                    </div>
                </div>

                <nav>
                    <ul class="pagination">
                    </ul>
                </nav>


            }
        </div>
    </div>
    <div class="modal fade" id="DeleteModal" tabindex="-1" role="dialog" aria-labelledby="DeleteModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="DeleteModalLabel">Delete Student</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    Are you sure that you want to delete user with such Id?
                </div>
                <div class="form-group row">
                    <label id="add_text" for="Id" class="col-sm-2 col-form-label text-left">Id</label>
                    <div class="col-sm-10">
                        <input name="Id" class="form-control">

                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <a type="button" class="btn btn-primary" asp-controller="Admin" asp-action="DeleteUser">Delete</a>
                </div>
            </div>
        </div>
    </div>
}*@

<table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Description)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Category)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Relevance)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Level)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Category)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Relevance)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Level)
                    </td>
                    <td>
                        <form asp-action="DeleteMasterClass" asp-route-id="@item.Id" method="post">
                            <a asp-action="EditMasterClasses" asp-controller="Logic" asp-route-id="@item.Id" class="btn btn-primary">Edit</a>
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
