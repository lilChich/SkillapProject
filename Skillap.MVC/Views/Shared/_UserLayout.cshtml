@{
    ViewData["Title"] = "MyAccount";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="row pb-5">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <ul class="nav nav-tabs card-header-tabs nav-fill">

                    @if (User.IsInRole("User"))
                    {
                        <li class="nav-item"><a id="main_text_authorization" class="nav-link" asp-controller="Account" asp-action="MyProfile">Profile</a></li>
                        <li class="nav-item"><a id="main_text_authorization" class="nav-link" asp-controller="Account" asp-action="MyStatistics">Statistics</a></li>
                        <li class="nav-item"><a id="main_text_authorization" class="nav-link" asp-controller="Account" asp-action="EditProfile">Settings</a></li>
                    }

                </ul>
            </div>

            <div class="card-body">
                <div class="container">
                    <div class="row">
                        @*<div class="col-3">
                            <div class="d-flex flex-column flex-shrink-0 p-3 bg-light">
                                <a href="#" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-decoration-none">
                                    <span class="fs-4">Content</span>

                                </a>

                                <hr />

                                <ul class="nav nav-pills flex-column mb-auto">
                                    <li class="nav-item">
                                        <a href="#" class="nav-link active">content</a>
                                    </li>
                                    <li class="nav-item">
                                        <a href="#" class="nav-link active">content</a>
                                    </li>
                                    <li class="nav-item">
                                        <a href="#" class="nav-link active">content</a>
                                    </li>
                                    <li class="nav-item">
                                        <a href="#" class="nav-link active">content</a>
                                    </li>
                                    <li class="nav-item">
                                        <a href="#" class="nav-link active">content</a>
                                    </li>
                                    <li class="nav-item">
                                        <a href="#" class="nav-link active">content</a>
                                    </li>
                                    <li class="nav-item">
                                        <a href="#" class="nav-link active">content</a>
                                    </li>
                                    <li class="nav-item">
                                        <a href="#" class="nav-link active">content</a>
                                    </li>
                                    <li class="nav-item">
                                        <a href="#" class="nav-link active">content</a>
                                    </li>
                                </ul>

                            </div>

                        </div>*@
                        <div class="col">
                            @RenderBody()
                        </div>
                    </div>
                </div>
            </div>        
        </div>
    </div>
</div>

@section Scripts {
    @RenderSection("Scripts", required: false)

    <script>
    $(function () {
        var current = location.pathname;
        $('.nav-tabs li a').each(function () {
            var $this = $(this);
            if (current.indexOf($this.attr('href')) !== -1) {
                $this.addClass('active');
            }
        })
    })
    </script>


}
